<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" role="img" aria-label="buildroot_">
  <!-- Blocky, brutalist "b" — thick squared stem + bowl, no curves, reads
       clearly at 16px favicon size. -->
  <path
    fill="#000000"
    fill-rule="evenodd"
    d="M7,3 L11,3 L11,13 L23,13 L23,23 L7,23 Z M15,16 L15,20 L19,20 L19,16 Z"
  />
  <!-- Trailing underscore — echoes the blinking `_` cursor after the
       "buildroot" wordmark in the header/logo. -->
  <rect x="6" y="26" width="18" height="3" fill="#000000" />
</svg>
